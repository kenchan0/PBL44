<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ubuntu.pbl44">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".choice" />
        <activity android:name=".sightseeing" />
        <activity android:name=".Restran" />
        <activity android:name=".Orion" />
        <activity android:name=".Cache" />
        <activity android:name=".Denden" />
        <activity android:name=".Hanadaikon" />
        <activity android:name=".Hanamizuki" />
        <activity android:name=".Hananoya" />
        <activity android:name=".Itamesi" />
        <activity android:name=".Marutetu" />
        <activity android:name=".Sichu" />
        <activity android:name=".Syokusoueno" />
        <activity android:name=".Torattoria" />
        <activity android:name=".Usagi" />
        <activity android:name=".countrypark" />
        <activity
            android:name=".countryparkmap"
            android:label="@string/title_activity_countryparkmap" />
        <activity android:name=".yuparesu" />
        <activity
            android:name=".yuparesumap"
            android:label="@string/title_activity_yuparesumap" />
        <activity android:name=".bentenyama" />
        <activity
            android:name=".bentenyamamap"
            android:label="@string/title_activity_bentenyamamap" />
        <activity android:name=".kosinoen" />
        <activity
            android:name=".kosinoenmap"
            android:label="@string/title_activity_kosinoenmap" />
        <activity android:name=".takabasiroato" />
        <activity
            android:name=".takabasiroatomap"
            android:label="@string/title_activity_takabasiroatomap" />
        <activity android:name=".myosenzi" />
        <activity
            android:name=".myosenzimap"
            android:label="@string/title_activity_myosenzimap" />
        <activity android:name=".hutago" />
        <activity
            android:name=".hutagomap"
            android:label="@string/title_activity_hutagomap" />
        <activity android:name=".takabahiyosi" />
        <activity
            android:name=".takabahiyosimap"
            android:label="@string/title_activity_takabahiyosimap" />
        <activity android:name=".vivre" />
        <activity
            android:name=".vivremap"
            android:label="@string/title_activity_vivremap"></activity>
    </application>

</manifest>